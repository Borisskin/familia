# nanobot baseline: 0.3.0
# upstream commit: 3f602fbc8c104b5af27aa4d3520e7dcef2fa70ec

diff --git aa/nanobot/nanobot/bus/runtime_events.py bb/nanobot/nanobot/bus/runtime_events.py
index fabe3c9..f7caf66 100644
--- aa/nanobot/nanobot/bus/runtime_events.py
+++ bb/nanobot/nanobot/bus/runtime_events.py
@@ -153,11 +153,20 @@ class RuntimeEventPublisher:
         session_key: str,
         metadata: dict[str, Any] | None,
     ) -> RuntimeEventContext:
+        safe_metadata = dict(metadata or {})
+        for key in (
+            "_runtime_request_context",
+            "_runtime_actor",
+            "_runtime_action",
+            "_runtime_inbound_channel",
+            "_runtime_inbound_chat_id",
+        ):
+            safe_metadata.pop(key, None)
         return RuntimeEventContext(
             channel=channel,
             chat_id=chat_id,
             session_key=session_key,
-            metadata=dict(metadata or {}),
+            metadata=safe_metadata,
         )
 
     def record_turn_runtime(self, session_key: str, runtime: Any) -> None:

