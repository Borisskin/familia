# nanobot baseline: 0.3.0
# upstream commit: 3f602fbc8c104b5af27aa4d3520e7dcef2fa70ec

diff --git aa/nanobot/nanobot/bus/events.py bb/nanobot/nanobot/bus/events.py
index def7703..e7e04de 100644
--- aa/nanobot/nanobot/bus/events.py
+++ bb/nanobot/nanobot/bus/events.py
@@ -32,6 +32,7 @@ class InboundMessage:
     media: list[str] = field(default_factory=list)  # Media URLs
     metadata: dict[str, Any] = field(default_factory=dict)  # Channel-specific data
     session_key_override: str | None = None  # Optional override for thread-scoped sessions
+    actor: str | None = None  # Server-authoritative principal, never client metadata
 
     @property
     def session_key(self) -> str:
@@ -56,3 +57,16 @@ class OutboundMessage:
     metadata: dict[str, Any] = field(default_factory=dict)
     buttons: list[list[str]] = field(default_factory=list)
     event: "OutboundEvent | None" = None
+
+
+@dataclass
+class CallbackEvent:
+    """Inline-keyboard callback retaining channel payload and server actor."""
+
+    channel: str
+    sender_id: str
+    chat_id: str
+    payload: Any
+    timestamp: datetime = field(default_factory=datetime.now)
+    actor: str | None = None
+    metadata: dict[str, Any] = field(default_factory=dict)

