# nanobot baseline: 0.3.5
# upstream commit: 1bb712d3488915ca4ed9ccc1a93067ff722f5ab9

diff --git a/nanobot/nanobot/bus/events.py b/nanobot/nanobot/bus/events.py
index 7d0f247..aa6633d 100644
--- a/nanobot/nanobot/bus/events.py
+++ b/nanobot/nanobot/bus/events.py
@@ -35,6 +35,7 @@
     session_key_override: str | None = None  # Optional override for thread-scoped sessions
     require_existing_session: bool = False
     input_role: Literal["user", "system"] | None = None
+    actor: str | None = None  # Server-authoritative identity after admission.
 
     @property
     def session_key(self) -> str:
@@ -66,3 +67,16 @@
     metadata: dict[str, Any] = field(default_factory=dict)
     buttons: list[list[str]] = field(default_factory=list)
     event: "AgentEvent | None" = None
+
+
+@dataclass
+class CallbackEvent:
+    """Typed channel callback kept separate from ordinary user messages."""
+
+    channel: str
+    sender_id: str
+    chat_id: str
+    payload: Any
+    timestamp: datetime = field(default_factory=datetime.now)
+    actor: str | None = None
+    metadata: dict[str, Any] = field(default_factory=dict)
