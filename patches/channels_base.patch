# nanobot baseline: 0.3.5
# upstream commit: 1bb712d3488915ca4ed9ccc1a93067ff722f5ab9

diff --git a/nanobot/nanobot/channels/base.py b/nanobot/nanobot/channels/base.py
index ed2fb77..5d17660 100644
--- a/nanobot/nanobot/channels/base.py
+++ b/nanobot/nanobot/channels/base.py
@@ -4,7 +4,7 @@ from __future__ import annotations
 
 from abc import ABC, abstractmethod
 from pathlib import Path
-from typing import Any, cast
+from typing import Any, Literal, cast
 
 from loguru import logger
 
@@ -18,6 +18,13 @@ from nanobot.pairing import (
 )
 
 
+DeliveryResult = Literal["delivered", "unavailable", "unknown"]
+
+
+class DeliveryUnavailableError(RuntimeError):
+    """The channel rejected a message before accepting it for delivery."""
+
+
 class BaseChannel(ABC):
     """
     Abstract base class for chat channel implementations.
@@ -114,9 +121,10 @@ class BaseChannel(ABC):
 
         Channels with protocol-level business errors can override this hook to
         prevent retries that cannot succeed until external state changes.
-        Transport and unexpected errors remain retryable by default.
+        Transport and unexpected errors remain retryable by default. Explicit
+        pre-delivery rejections are not retried.
         """
-        return True
+        return not isinstance(error, DeliveryUnavailableError)
 
     def start_error_message(self, error: Exception) -> str | None:
         """Return an actionable public message for a channel startup failure.
